---
- hosts: localhost
  become: true
  tasks:
  - name: Update.
    apt:
      upgrade: dist
      update_cache: yes
    when: ansible_distribution == "Ubuntu"
  - name: Install basic packages.
    apt:
      name:
        - htop
        - moc
      state: latest
    when: ansible_distribution == "Ubuntu"
